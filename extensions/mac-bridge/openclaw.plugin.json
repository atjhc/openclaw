{
  "id": "mac-bridge",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["url"],
    "properties": {
      "url": {
        "type": "string",
        "description": "Base URL of the unified MacBridge server (e.g. http://192.168.2.1:7330)"
      }
    }
  }
}
